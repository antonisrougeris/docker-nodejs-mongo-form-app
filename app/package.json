{
  "name": "docker-nodejs-mongo-form-app",
  "version": "1.0.0",
  "description": "A simple Node.js form application connected to MongoDB, running in Docker containers using Docker Compose.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Antonis Rougeris",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongodb": "^3.3.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
