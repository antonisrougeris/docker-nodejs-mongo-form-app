# 🐳 Node.js Form App with MongoDB using Docker

This project is a simple **Node.js form-based web application** connected to **MongoDB**, both running inside **Docker containers**.  
It demonstrates how to containerize a Node.js + MongoDB stack using **Docker Compose**, and how to store your custom image in a **private AWS ECR repository**.

---

## 📘 Overview

The project includes:

- 🧩 **Node.js + Express** backend server  
- 🗄️ **MongoDB** database for data storage  
- 🧱 **Docker Compose** for multi-container management  
- ☁️ **AWS ECR** for hosting private Docker images  

The app features a simple form (front-end HTML) that connects to a Node.js backend API, which in turn interacts with MongoDB for storing or retrieving data.

---

## 📂 Project Structure

